# Donut (Node.js Port)

3D ASCII donut animation rendered in the terminal, migrated directly from `source/donut.py`.

## Requirements
- **OS:** Ubuntu 24.04 (or any POSIX-compliant terminal environment)
- **Runtime:** Node.js (v18 or higher recommended)
- **Standard Library Only:** Zero external npm package dependencies.

## How to Run

1. Navigate to the `target/` directory:
   ```bash
   cd target